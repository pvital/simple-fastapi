[project]
name = "simple-fastapi"
version = "0.1.0"
description = "Simple FastAPI app."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "instana>=3.7.2",
    "psutil>=7.0.0",
]
